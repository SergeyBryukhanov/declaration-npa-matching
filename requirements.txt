# Базовые зависимости для run.py (офлайн-инференс)
numpy==1.26.4
pandas==2.2.2

# Русская лемматизация для BM25-токенизации (опционально: при отсутствии
# пакета пайплайн тихо переключается на нормализацию без лемматизации -
# см. src/text_normalize.py). Рекомендуется ставить для качества.
pymorphy2==0.9.1
pymorphy2-dicts-ru==2.4.417127.4579844

# Плотные эмбеддинги (intfloat/multilingual-e5-small)
sentence-transformers==3.0.1
torch==2.3.1

# Локальный инференс Qwen2.5-7B-Instruct в GGUF (CPU и/или GPU)
llama-cpp-python==0.3.35

# --- Только для prepare.py (одноразовая подготовка среды, сеть разрешена) ---
huggingface-hub==0.24.6
