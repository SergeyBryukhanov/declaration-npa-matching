# Базовые зависимости для run.py (офлайн-инференс)
numpy==1.26.4
pandas==2.2.2
tqdm==4.66.5

# Русская лемматизация для BM25-токенизации (опционально: при отсутствии
# пакета пайплайн тихо переключается на нормализацию без лемматизации -
# см. src/text_normalize.py). Рекомендуется ставить для качества.
pymorphy2==0.9.1
pymorphy2-dicts-ru==2.4.417127.4579844

# Плотные эмбеддинги (intfloat/multilingual-e5-small)
sentence-transformers==3.0.1
torch==2.3.1

# --- llama-cpp-python устанавливается ОТДЕЛЬНО, не отсюда ---
# Причина (проблемы №3-5 из README, раздел "Диагностика проблем установки"):
# для этого пакета не всегда есть готовый Windows-wheel под конкретную пару
# (версия пакета x версия Python x CPU/CUDA), и pip, не найдя точного
# совпадения, тихо уходит в сборку из исходников, которая на машине без
# компилятора C++ падает - а поскольку `pip install -r requirements.txt`
# ставит все пакеты одной транзакцией, один упавший пакет откатывает
# ВООБЩЕ ВСЁ, включая уже готовые к установке numpy/torch/sentence-transformers.
# Поэтому его ставят отдельной командой (см. install.ps1 или README, раздел
# "Установка" / "Использование GPU") ПОСЛЕ этого файла, с явным выбором
# CPU- или CUDA-сборки под конкретное железо.
#
# llama-cpp-python==0.3.35  # см. install.ps1

# --- Только для prepare.py (одноразовая подготовка среды, сеть разрешена) ---
huggingface-hub==0.24.6

# --- Прямая зависимость llama-cpp-python (используется при импорте для кеша
# промптов), которую НЕ подтягивает `pip install llama-cpp-python --no-deps`.
# Ставим отдельно здесь: пакет маленький и чисто-питоновский, без риска
# конфликта версий (в отличие от самого llama-cpp-python).
diskcache>=5.6.1
